# playbooks/

自分たちの作業に直接使える実践レシピ。「明日からこの手順で動ける」レベルに落とし込んだものを置く。

`frameworks/` や `topics/` が「知識」なのに対し、こちらは「手順」。たとえば：

- 新規リポジトリに Claude Code を導入するときのチェックリスト
- 既存コードベースに対する `/init` の進め方
- レビュー・セキュリティレビューの運用フロー
- メモリ／CLAUDE.md の育て方
